#include<bits/stdc++.h>
#define fastio ios_base::sync_with_stdio(false);;cin.tie(NULL);cout.tie(NULL);
/**Sum of even numbers will always be even number*/
using namespace std;
int main()
{
    fastio;
    int w;
    cin>>w;
    if(w&1)
        cout<<"NO"<<"\n";
    else
        cout<<"YES"<<"\n";
   return 0;
}
//Evan
